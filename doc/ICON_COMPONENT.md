# Icon Component · Документация

> **Универсальный компонент иконки для Gutenberg блоков**
> **Версия:** 1.0.0
> **Создан:** 2025-12-02

---

## 📖 Содержание

1. [Обзор](#обзор)
2. [Возможности](#возможности)
3. [Структура файлов](#структура-файлов)
4. [Использование](#использование)
5. [Атрибуты блока Icon](#атрибуты-блока-icon)
6. [Компоненты](#компоненты)
7. [Утилиты](#утилиты)
8. [Примеры использования](#примеры-использования)
9. [Технические детали](#технические-детали)

---

## Обзор

**Icon Component** — универсальный компонент для работы с иконками в Gutenberg блоках, поддерживающий:
- Font Icons (Unicons) — `<i class="uil uil-{name}">`
- SVG Icons (lineal, solid, solid-mono, solid-duo) — встроенные SVG из темы
- Custom SVG — загрузка через Media Library

Компонент состоит из:
- **Блока Icon** (`codeweber-blocks/icon`) — самостоятельный блок для вставки иконок
- **Переиспользуемых компонентов** — для использования в других блоках

---

## Возможности

### Типы иконок

1. **Font Icons (Unicons)**
   - 1000+ векторных иконок из библиотеки Unicons
   - Поддержка размеров из темы (`fs-14`, `fs-28`, `fs-40` и т.д.)
   - Цветовая палитра темы

2. **SVG Icons**
   - **Lineal** — контурные иконки
   - **Solid** — заливка одним цветом
   - **Solid Mono** — монохромная заливка
   - **Solid Duo** — двухцветная заливка (primary/secondary)
   - Размеры: `xs`, `sm`, `md`, `lg`

3. **Custom SVG**
   - Загрузка через WordPress Media Library
   - Поддержка всех настроек как у встроенных SVG

### Обёртка div.icon

Опциональная обёртка с тремя стилями:

1. **Div.icon** — базовая обёртка для позиционирования
2. **Square** — квадратная кнопка (`btn btn-block`)
3. **Round** — круглая кнопка (`btn btn-block btn-circle`)

Для кнопок доступны:
- **Варианты:** Soft, Outline, Solid
- **Размеры:** Btn-md, Btn-sm, Btn-lg
- **Цвета:** все цвета палитры темы

### Настройки

- **Цвет** — палитра темы (20+ цветов)
- **Размер** — адаптивные размеры из темы
- **Дополнительные классы** — для иконки и обёртки раздельно
- **Выравнивание** — left, center, right
- **Block Meta** — custom class, data-атрибуты, ID

---

## Структура файлов

```
src/
├── blocks/icon/                    # Блок Icon
│   ├── block.json                  # Метаданные и атрибуты
│   ├── index.js                    # Регистрация блока
│   ├── edit.js                     # UI редактора
│   ├── save.js                     # HTML-вывод
│   ├── sidebar.js                  # Табы настроек
│   ├── editor.scss                 # Стили редактора
│   └── style.scss                  # Стили фронтенда
│
├── components/icon/                # Переиспользуемые компоненты
│   ├── IconControl.js              # Inspector Control
│   ├── IconPicker.js               # Модальное окно выбора иконки
│   ├── IconRender.js               # Рендеринг иконки (editor + save)
│   ├── InlineSvg.js                # Inline SVG для редактора
│   ├── editor.scss                 # Стили компонентов
│   └── index.js                    # Экспорты и хелперы
│
└── utilities/
    ├── icon_sizes.js               # Константы размеров, цветов, стилей
    └── svg_icons.js                # Массивы SVG иконок и хелперы
```

---

## Использование

### В блоке Icon

Блок **Icon** (`codeweber-blocks/icon`) — готовый к использованию блок для вставки иконок.

**Где найти:** Gutenberg Editor → Codeweber Gutenberg Blocks → Icon

**Интерфейс:**
- **Таб Icon** — выбор типа, иконки, цвета, размера
- **Таб Wrapper** — настройки обёртки div.icon
- **Таб Settings** — Block Class, Block Data, Block ID

### В других блоках

Для добавления настроек иконки в свой блок:

#### 1. Добавить атрибуты в block.json

```json
{
  "attributes": {
    ...iconAttributes('icon')
  }
}
```

Где `'icon'` — префикс атрибутов (можно пустую строку `''`).

#### 2. Использовать IconControl в edit.js

```jsx
import { IconControl } from '../../components/icon';

<InspectorControls>
  <IconControl
    attributes={attributes}
    setAttributes={setAttributes}
    prefix="icon"
    label={__('Icon Settings', 'codeweber-blocks')}
    allowSvg={true}
    allowFont={true}
    allowCustom={true}
    showWrapper={true}
    initialOpen={true}
  />
</InspectorControls>
```

#### 3. Рендерить иконку

```jsx
import { IconRender } from '../../components/icon';

// В edit.js
<IconRender
  {...attributes}
  isEditor={true}
/>

// В save.js
import { IconRenderSave } from '../../components/icon';

<IconRenderSave {...attributes} />
```

---

## Атрибуты блока Icon

| Атрибут | Тип | По умолчанию | Описание |
|---------|-----|--------------|----------|
| `iconType` | `string` | `'font'` | Тип иконки: `'font'`, `'svg'`, `'custom'`, `'none'` |
| `iconName` | `string` | `'star'` | Имя Font иконки (без префикса `uil-`) |
| `svgIcon` | `string` | `''` | Имя SVG иконки |
| `svgStyle` | `string` | `'lineal'` | Стиль SVG: `'lineal'`, `'solid'`, `'solid-mono'`, `'solid-duo'` |
| `iconSize` | `string` | `'xs'` | Размер SVG: `'xs'`, `'sm'`, `'md'`, `'lg'` |
| `iconFontSize` | `string` | `'fs-40'` | Размер Font: `'fs-14'`, `'fs-28'`, `'fs-40'` и т.д. |
| `iconColor` | `string` | `'primary'` | Основной цвет из палитры |
| `iconColor2` | `string` | `''` | Второй цвет (для solid-duo) |
| `iconClass` | `string` | `''` | Дополнительные классы иконки |
| `iconWrapper` | `boolean` | `false` | Обернуть в div.icon |
| `iconWrapperStyle` | `string` | `''` | Стиль обёртки: `''`, `'btn'`, `'btn-circle'` |
| `iconBtnSize` | `string` | `''` | Размер кнопки: `''`, `'btn-sm'`, `'btn-lg'` |
| `iconBtnVariant` | `string` | `'soft'` | Вариант кнопки: `'soft'`, `'outline'`, `'solid'` |
| `iconWrapperClass` | `string` | `''` | Классы обёртки |
| `customSvgUrl` | `string` | `''` | URL кастомного SVG |
| `customSvgId` | `number` | `0` | ID из Media Library |
| `blockAlign` | `string` | `''` | Выравнивание: `'left'`, `'center'`, `'right'` |
| `blockClass` | `string` | `''` | Дополнительный класс блока |
| `blockData` | `string` | `''` | Data-атрибуты (`key=value,key2=value2`) |
| `blockId` | `string` | `''` | ID блока |

---

## Компоненты

### IconControl

**Назначение:** Inspector Control для настроек иконки

**Расположение:** `src/components/icon/IconControl.js`

**Props:**
```typescript
{
  attributes: Object,           // Атрибуты блока
  setAttributes: Function,      // Функция обновления
  prefix: string,               // Префикс атрибутов ('icon', '' и т.д.)
  label: string,                // Заголовок панели
  allowSvg: boolean,            // Разрешить SVG
  allowFont: boolean,           // Разрешить Font Icons
  allowCustom: boolean,         // Разрешить Custom SVG
  showWrapper: boolean,         // Показать настройки обёртки
  initialOpen: boolean,         // Открыта ли панель по умолчанию
}
```

**Функционал:**
- Выбор типа иконки
- Кнопка открытия IconPicker
- Настройки размера, цвета, стиля
- Превью выбранной иконки
- Загрузка Custom SVG
- Настройки обёртки (если `showWrapper: true`)

---

### IconPicker

**Назначение:** Модальное окно для выбора иконки

**Расположение:** `src/components/icon/IconPicker.js`

**Props:**
```typescript
{
  isOpen: boolean,              // Открыто ли окно
  onClose: Function,            // Callback закрытия
  onSelect: Function,           // Callback выбора иконки
  currentIcon: Object,          // Текущая иконка
  allowFont: boolean,           // Разрешить Font Icons
  allowSvg: boolean,            // Разрешить SVG
}
```

**Функционал:**
- Табы: Font Icons, SVG Lineal, SVG Solid
- Поиск по названию иконки
- Превью иконок
- Кнопка "Очистить" (установить `iconType: 'none'`)

---

### IconRender

**Назначение:** Рендеринг иконки (editor + frontend)

**Расположение:** `src/components/icon/IconRender.js`

**Props:**
```typescript
{
  iconType: string,             // Тип иконки
  iconName: string,             // Имя Font иконки
  svgIcon: string,              // Имя SVG иконки
  svgStyle: string,             // Стиль SVG
  iconSize: string,             // Размер SVG
  iconFontSize: string,         // Размер Font
  iconColor: string,            // Основной цвет
  iconColor2: string,           // Второй цвет (duo)
  iconClass: string,            // Классы иконки
  iconWrapper: boolean,         // Обернуть в div.icon
  iconWrapperStyle: string,     // Стиль обёртки
  iconBtnSize: string,          // Размер кнопки
  iconBtnVariant: string,       // Вариант кнопки
  iconWrapperClass: string,     // Классы обёртки
  customSvgUrl: string,         // URL Custom SVG
  customSvgId: number,          // ID Custom SVG
  blockAlign: string,           // Выравнивание (для save)
  blockClass: string,           // Класс блока (для save)
  blockData: string,            // Data-атрибуты (для save)
  blockId: string,              // ID блока (для save)
  isEditor: boolean,            // Рендеринг в редакторе
}
```

**Экспорты:**
- `IconRender` — для использования в edit.js и save.js
- `IconRenderSave` — алиас для save.js (с `isEditor: false`)

**Логика:**
- Генерирует HTML в зависимости от типа иконки
- Применяет классы размеров и цветов
- Обёртывает в `div.icon` при необходимости
- На фронтенде применяет Block Meta к обёртке или иконке

---

### InlineSvg

**Назначение:** Inline SVG для редактора (поддержка CSS цветов)

**Расположение:** `src/components/icon/InlineSvg.js`

**Props:**
```typescript
{
  src: string,                  // URL SVG файла
  className: string,            // CSS классы
  alt: string,                  // Alt текст
  preserveFillClasses: boolean, // Сохранять fill-primary/secondary
}
```

**Особенности:**
- Загружает SVG через fetch
- Вставляет inline в DOM
- Поддерживает `fill="currentColor"` для CSS цветов
- Для solid-иконок сохраняет `fill-primary`/`fill-secondary` классы
- Кэширование запросов

---

## Утилиты

### icon_sizes.js

**Расположение:** `src/utilities/icon_sizes.js`

**Экспорты:**
```javascript
// Типы иконок
iconTypes: [
  { value: 'none', label: 'None' },
  { value: 'font', label: 'Font Icon (Unicons)' },
  { value: 'svg', label: 'SVG Icon' },
  { value: 'custom', label: 'Custom SVG' },
]

// Размеры SVG
iconSvgSizes: [
  { value: 'xs', label: 'XS (icon-svg-xs)' },
  { value: 'sm', label: 'SM (icon-svg-sm)' },
  { value: 'md', label: 'MD (icon-svg-md)' },
  { value: 'lg', label: 'LG (icon-svg-lg)' },
]

// Размеры Font (из темы)
iconFontSizes: [
  { value: 'fs-14', label: 'fs-14' },
  { value: 'fs-16', label: 'fs-16' },
  { value: 'fs-18', label: 'fs-18' },
  { value: 'fs-20', label: 'fs-20' },
  { value: 'fs-22', label: 'fs-22' },
  { value: 'fs-24', label: 'fs-24' },
  { value: 'fs-28', label: 'fs-28' },
  { value: 'fs-40', label: 'fs-40' },
]

// Цвета (палитра темы)
iconColors: [
  { value: 'primary', label: 'Primary' },
  { value: 'grape', label: 'Grape' },
  { value: 'violet', label: 'Violet' },
  // ... 20+ цветов
]

// Duo-цвета (для solid-duo)
iconDuoColors: [
  { value: 'grape-violet', label: 'Grape / Violet' },
  { value: 'violet-blue', label: 'Violet / Blue' },
  // ... комбинации
]

// Стили SVG
svgIconStyles: [
  { value: 'lineal', label: 'Lineal (контур)' },
  { value: 'solid', label: 'Solid (заливка)' },
  { value: 'solid-mono', label: 'Solid Mono (монохром)' },
  { value: 'solid-duo', label: 'Solid Duo (два цвета)' },
]

// Стили обёртки
iconWrapperStyles: [
  { value: '', label: 'Div.icon' },
  { value: 'btn', label: 'Square' },
  { value: 'btn-circle', label: 'Round' },
]

// Размеры кнопок
iconBtnSizes: [
  { value: '', label: 'Btn-md' },
  { value: 'btn-sm', label: 'Btn-sm' },
  { value: 'btn-lg', label: 'Btn-lg' },
]

// Варианты кнопок
iconBtnVariants: [
  { value: 'soft', label: 'Soft' },
  { value: 'outline', label: 'Outline' },
  { value: 'solid', label: 'Solid' },
]
```

---

### svg_icons.js

**Расположение:** `src/utilities/svg_icons.js`

**Экспорты:**
```javascript
// Массивы SVG иконок
svgIconsLineal: ['award', 'briefcase', 'camera', ...]  // 200+ иконок
svgIconsSolid: ['alarm', 'article', 'basket', ...]     // 100+ иконок

// Хелпер для получения URL
getSvgIconPath(iconName, style)
// → '/wp-content/themes/codeweber/dist/assets/img/icons/{style}/{iconName}.svg'
```

**Путь к SVG в теме:**
```
/wp-content/themes/codeweber/dist/assets/img/icons/
├── lineal/
│   ├── award.svg
│   ├── briefcase.svg
│   └── ...
└── solid/
    ├── alarm.svg
    ├── article.svg
    └── ...
```

---

## Примеры использования

### Пример 1: Font Icon без обёртки

**Настройки:**
- Тип: Font Icon
- Иконка: `star`
- Размер: `fs-40`
- Цвет: `primary`
- Дополнительный класс: `me-2`

**HTML на фронтенде:**
```html
<i class="uil uil-star fs-40 text-primary me-2"></i>
```

---

### Пример 2: SVG Lineal с обёрткой

**Настройки:**
- Тип: SVG Icon
- Иконка: `award` (lineal)
- Размер: `md`
- Цвет: `grape`
- Обёртка: Div.icon
- Классы обёртки: `mb-4`

**HTML на фронтенде:**
```html
<div class="icon text-grape mb-4">
  <img src="/wp-content/themes/codeweber/dist/assets/img/icons/lineal/award.svg" 
       class="svg-inject icon-svg icon-svg-md text-grape" 
       alt="">
</div>
```

---

### Пример 3: SVG Solid Duo с кнопкой

**Настройки:**
- Тип: SVG Icon
- Иконка: `alarm` (solid)
- Стиль: Solid Duo
- Цвета: `grape-violet`
- Размер: `lg`
- Обёртка: Round
- Вариант: Soft
- Размер кнопки: Btn-lg

**HTML на фронтенде:**
```html
<div class="icon btn btn-block btn-circle btn-soft-grape btn-lg">
  <img src="/wp-content/themes/codeweber/dist/assets/img/icons/solid/alarm.svg" 
       class="svg-inject icon-svg icon-svg-lg solid-duo text-grape-violet" 
       alt="">
</div>
```

---

### Пример 4: Font Icon с Block Meta

**Настройки:**
- Тип: Font Icon
- Иконка: `phone-volume`
- Размер: `fs-28`
- Цвет: `blue`
- Обёртка: Square
- Вариант: Outline
- Block Class: `my-custom-class`
- Block ID: `contact-phone`
- Block Data: `action=call,target=mobile`

**HTML на фронтенде:**
```html
<div class="icon btn btn-block btn-outline-blue my-custom-class" 
     id="contact-phone" 
     data-action="call" 
     data-target="mobile">
  <i class="uil uil-phone-volume"></i>
</div>
```

---

## Технические детали

### Различия редактор / фронтенд

| Аспект | Редактор | Фронтенд |
|--------|----------|----------|
| **SVG рендеринг** | `InlineSvg` (inline) | `<img>` с классом `svg-inject` |
| **CSS цвета** | `fill="currentColor"` работает | SVGInject библиотека заменяет на inline |
| **Обёртка блока** | `<div class="cwgb-icon-block">` | Без обёртки |
| **Block Meta** | Не применяется | На обёртке или иконке |

### SVG стили и цвета

| Стиль | Цвет | Классы | Как работает |
|-------|------|--------|--------------|
| `lineal` | Один | `text-{color}` | `fill="currentColor"` |
| `solid` | Один | `solid text-{color}` | `.solid.text-{color} .fill-primary` в CSS |
| `solid-mono` | Один | `solid-mono text-{color}` | `.solid-mono.text-{color} .fill-primary` в CSS |
| `solid-duo` | Два | `solid-duo text-{color1}-{color2}` | `.solid-duo.text-{c1}-{c2} .fill-primary/secondary` в CSS |

### Логика применения классов

**iconClass (Дополнительный класс иконки):**
- Всегда применяется к самой иконке (`<i>`, `<img>`)
- Даже если включена обёртка

**iconWrapperClass (Доп. классы обёртки):**
- Применяется только к `div.icon`
- Игнорируется, если обёртка отключена

**blockClass, blockAlign, blockData, blockId:**
- Применяются к `div.icon`, если обёртка включена
- Применяются к иконке (`<i>`) или `<span>`, если обёртки нет

### Обёртка div.icon — варианты стилей

**Div.icon (без кнопки):**
```html
<div class="icon text-{color} {iconWrapperClass} {blockClass}">
  <i class="uil uil-{name} {iconClass}"></i>
</div>
```

**Square (квадратная кнопка):**
```html
<div class="icon btn btn-block btn-{variant}-{color} btn-{size} {iconWrapperClass} {blockClass}">
  <i class="uil uil-{name} {iconClass}"></i>
</div>
```

**Round (круглая кнопка):**
```html
<div class="icon btn btn-block btn-circle btn-{variant}-{color} btn-{size} {iconWrapperClass} {blockClass}">
  <i class="uil uil-{name} {iconClass}"></i>
</div>
```

Варианты кнопок: `btn-soft-{color}`, `btn-outline-{color}`, `btn-{color}`

---

## История разработки

### Основные этапы

1. **Создание базового компонента** (начало)
   - IconControl, IconPicker, IconRender
   - Поддержка Font Icons и SVG Icons
   - Базовые настройки (тип, размер, цвет)

2. **Добавление Custom SVG**
   - Загрузка через Media Library
   - Поддержка всех настроек как у встроенных SVG

3. **Реализация обёртки div.icon**
   - Добавлен ToggleControl для включения/выключения
   - Настройки стиля обёртки (div.icon, button, round-button)
   - Дополнительные классы обёртки

4. **Стили кнопок**
   - Варианты: Soft, Outline, Solid
   - Размеры: Btn-md, Btn-sm, Btn-lg
   - Интеграция с цветовой палитрой темы

5. **Добавление Block Meta**
   - Интеграция BlockMetaFields (class, data, id)
   - Применение Block Meta к обёртке или иконке

6. **Создание блока Icon**
   - Регистрация блока в `inc/Plugin.php`
   - edit.js, save.js с поддержкой всех настроек
   - Табы: Icon, Wrapper, Settings

7. **UI/UX улучшения**
   - TabPanel вместо отдельных PanelBody
   - ButtonGroup вместо SelectControl для стилей
   - Иконки табов в едином стиле с другими блоками
   - Удаление PanelBody из табов для единообразия

8. **Оптимизация фронтенда**
   - Удаление внешней обёртки блока на фронтенде
   - Block Meta применяется к обёртке или непосредственно к иконке
   - Раздельное применение iconClass и iconWrapperClass

9. **Финальные правки**
   - iconClass всегда на самой иконке, даже с обёрткой
   - Переименования лейблов (Div.icon, Square, Round)
   - Удаление функционала анимации (перенесён в другие блоки)

### Технические решения

**Проблема:** SVG иконки не отображались в редакторе
**Решение:** Создан компонент `InlineSvg`, который загружает SVG через fetch и вставляет inline, позволяя CSS стилям работать с `fill="currentColor"`

**Проблема:** Solid SVG иконки не меняли цвет
**Решение:** Сохранение классов `fill-primary` и `fill-secondary` внутри SVG, добавление соответствующих CSS правил в `editor.scss`

**Проблема:** Неясное разделение классов иконки и обёртки
**Решение:** Четкое разделение: `iconClass` → иконка, `iconWrapperClass` → обёртка, независимо от наличия обёртки

**Проблема:** Лишняя обёртка блока на фронтенде
**Решение:** Удаление `useBlockProps` из `save.js`, применение Block Meta напрямую к обёртке/иконке через `IconRender`

---

## Интеграция с другими блоками

Компонент Icon спроектирован для использования в других блоках. Примеры интеграции см. в разделе [Использование](#использование).

**Рекомендации:**
1. Используйте префикс для атрибутов (например, `'leftIcon'`, `'rightIcon'`)
2. Импортируйте `iconAttributes` из `src/components/icon`
3. Передавайте все необходимые props в `IconControl` и `IconRender`
4. Используйте `isEditor` для различия редактор/фронтенд

---

## Поддержка и обновления

**Разработчик:** AI Agent (Claude Sonnet 4.5)
**Дата создания:** 2025-12-02
**Версия плагина:** 0.1.0

При обновлении темы `codeweber` проверьте:
- Новые иконки в `/dist/assets/img/icons/`
- Изменения классов размеров (`fs-*`, `icon-svg-*`)
- Обновления цветовой палитры

---

> **Последнее обновление:** 2025-12-02










