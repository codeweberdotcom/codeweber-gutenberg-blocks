/**
 * Social Icons Block - Save Component
 *
 * Block uses PHP render (render.php), so we return null.
 *
 * @package CodeWeber Gutenberg Blocks
 */

const SocialIconsSave = () => {
	return null;
};

export default SocialIconsSave;
