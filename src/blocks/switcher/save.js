/**
 * Switcher Block - Save Component
 *
 * Dynamic block: the frontend is rendered by render.php so the active item can
 * be matched against the URL of the page actually being viewed.
 */

const SwitcherSave = () => null;

export default SwitcherSave;
